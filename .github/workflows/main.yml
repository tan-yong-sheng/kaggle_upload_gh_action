name: Upload to Kaggle

on:
  push:
    branches:
      - main

jobs:
  upload:
    name: Upload to Kaggle
    runs-on: ubuntu-latest

    steps:
      - name: Get directory
        run: ls -a

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8

      - name: Install Dependencies
        run: |
          pip install kaggle

      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Upload to Kaggle
        uses: harupy/push-kaggle-kernel@master
        env:
          KAGGLE_USERNAME: ${{ secrets.KAGGLE_USERNAME }}
          KAGGLE_KEY: ${{ secrets.KAGGLE_KEY }}
          KERNEL_SLUG: ${{ env.KERNEL_SLUG }}
        with:
          id: ${{ secrets.KAGGLE_USERNAME }}
          title: ${{ env.KERNEL_SLUG }}
          code_file: /project/script.py  # Update the path accordingly
          language: python
          kernel_type: script
          is_private: false
