name: Upload

on:
  push:
    branches:
      - master

jobs:
  upload:
    name: Upload
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Create a toy python script
        run: |
          echo "print('hello world')" > script.py

      - uses: harupy/push-kaggle-kernel@master
        env:
          # Do not leak your credentials.
          KAGGLE_USERNAME: ${{ secrets.KAGGLE_USERNAME }}
          KAGGLE_KEY: ${{ secrets.KAGGLE_KEY }}
          KERNEL_SLUG: ${{ env.KERNEL_SLUG }}
        with:
          id: ${{ secrets.KAGGLE_USERNAME }}/${{ env.KERNEL_SLUG }}
          title: ${{ env.KERNEL_SLUG }}
          code_file: kaggle-upload-gh-action.ipynb
          language: python
          kernel_type: script
          # Do not share high-scoring kernels.
          is_private: false